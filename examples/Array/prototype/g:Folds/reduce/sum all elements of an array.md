# Sum all elements of an Array

```js
var elements = [1, 2, 3, 4, 5, 6]
var sum = elements.reduce(function(result, value) {
                            return result + value
                          }, 0)
```
